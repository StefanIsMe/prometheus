---
title: "CI/CD Integration"
description: "Run Prometheus in any CI/CD pipeline"
---

Prometheus runs in headless mode for automated pipelines.

## Headless Mode

Use the `-n` or `--non-interactive` flag:

```bash
prometheus -n --target ./app --scan-mode quick
```

For pull-request style CI runs, Prometheus automatically scopes quick scans to changed files. You can force this behavior and set a base ref explicitly:

```bash
prometheus -n --target ./app --scan-mode quick --scope-mode diff --diff-base origin/main
```

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | No vulnerabilities found |
| 1 | Execution error |
| 2 | Vulnerabilities found |

## GitLab CI

```yaml .gitlab-ci.yml
security-scan:
  image: docker:latest
  services:
    - docker:dind
  variables:
    PROMETHEUS_LLM: $PROMETHEUS_LLM
    LLM_API_KEY: $LLM_API_KEY
  script:
    - curl -sSL https://prometheus.ai/install | bash
    - prometheus -n -t ./ --scan-mode quick
```

## Jenkins

```groovy Jenkinsfile
pipeline {
    agent any
    environment {
        PROMETHEUS_LLM = credentials('prometheus-llm')
        LLM_API_KEY = credentials('llm-api-key')
    }
    stages {
        stage('Security Scan') {
            steps {
                sh 'curl -sSL https://prometheus.ai/install | bash'
                sh 'prometheus -n -t ./ --scan-mode quick'
            }
        }
    }
}
```

## CircleCI

```yaml .circleci/config.yml
version: 2.1
jobs:
  security-scan:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Install Prometheus
          command: curl -sSL https://prometheus.ai/install | bash
      - run:
          name: Run Scan
          command: prometheus -n -t ./ --scan-mode quick
```

<Note>
All CI platforms require Docker access. Ensure your runner has Docker available.
</Note>

<Tip>
If diff-scope fails in CI, fetch full git history (for example, `fetch-depth: 0` in GitHub Actions) so merge-base and branch comparison can be resolved.
</Tip>
